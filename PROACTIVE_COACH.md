# 🚀 ارتقای مربی پیش‌دستانه ۲۴/۷ (Proactive AI Coach)

این نسخه، ربات را از یک «ربات واکنشی ساده» به یک **مربی پیش‌دستانهٔ ۲۴/۷** تبدیل می‌کند که رفتار کاربر را رصد می‌کند و در لحظهٔ مناسب، خودش اقدام می‌کند.

## ✨ چهار قابلیت اصلی

### ۱. Miss Patrol — مراقبت از افت زنجیرهٔ عادت
- تشخیص **۲ روز متوالی یا بیشتر عدم انجام** هر عادت.
- ارسال پیام تعاملی تلگرام با دکمه‌های:
  - ✅ انجام دادم (ثبت فوری عادت)
  - 💬 حرف بزنیم (ورود به مربی تعاملی)
  - ⏰ بعداً یادآوری کن
- استناد علمی به مدل **لالی (۲۰۱۰)**: نقطهٔ حساس شکستن مسیر = ۲ روز پشت‌سرهم.

### ۲. Timing Engine — موتور زمان‌بندی و «پنجرهٔ طلایی»
- یادگیری الگوی ساعتی هر عادت از روی `completedTimestamps` و `completionTimes`.
- تشخیص خودکار **پنجرهٔ طلایی** (ساعتی که کاربر بیشترین احتمال انجام را دارد).
- ارسال یادآوریِ درست در همان بازه، نه در زمان دلبخواه.

### ۳. Relapse Risk — پیش‌بینی و هشدار پیش از لغزش
- امتیاز خطر (۰–۱۰۰) بر پایهٔ: عادات کم‌خودکارشدگی انجام‌نشده، افت‌های متوالی، تسک‌های معوق، ساعت پرخطر شبانه.
- سطوح: `low` / `medium` / `high`.
- پیام هشدار **قبل از وقوع لغزش**، همراه با دلایل.

### ۴. Overdue Task Guard — نگهبان تسک‌های عقب‌افتاده
- تشخیص تسک‌هایی که از مهلتشان گذشته.
- ارسال یادآوری با اولویت مشخص.

### ＋ حلقهٔ یادگیری تطبیقی (Adaptive Learning Loop)
- ثبت واکنش کاربر به هر پیام: `acted` / `snoozed` / `ignored` / `dismissed`.
- محاسبهٔ نرخ پاسخ، بهترین ساعت پاسخ، و تنظیم خودکار شدت و لحن.
- سه سطح شدت: 🌿 ملایم / ⚖️ متعادل / ⚡ سختگیر.
- سقف پیام روزانه (پیش‌فرض ۳) + ساعات سکوت اختیاری.

## 🧩 فایل‌های جدید

| فایل | نقش |
|---|---|
| `server/behaviorObserver.ts` | مغز تحلیل رفتار (تشخیص افت، پنجرهٔ طلایی، ریسک لغزش، تصمیم‌گیری، یادگیری) |
| `server/proactiveEngine.ts` | لایهٔ اجرا (قالب‌بندی پیام سه‌زبانه، ارسال، ثبت بازخورد، پل runtime) |

## 🔌 نقاط ادغام در `server.ts`

- سرویس چرخهٔ پیش‌دستانه هر **۱۰ دقیقه** (`runProactiveCycle`).
- پردازش callbackهای `proact_*` (انجام دادم / حرف بزنیم / بعداً).
- دستور تلگرام `/proactive` + زیرمنو (روشن/خاموش، آمار، شدت).
- ثبت در **Telegram Bot Commands**.
- افزودن فیلدهای `behaviorEngine` و `proactiveCoach` به ذخیره‌سازی پایدار.

## 🌐 API جدید

| متد | مسیر | کارکرد |
|---|---|---|
| GET | `/api/proactive/status` | وضعیت + آمار + ریسک لغزش |
| GET | `/api/proactive/config` | دریافت تنظیمات |
| POST | `/api/proactive/config` | بروزرسانی تنظیمات |
| POST | `/api/proactive/run` | اجرای دستی چرخه (تست) |
| GET | `/api/proactive/preview` | پیش‌نمایش اقدامات (بدون ارسال) |
| POST | `/api/proactive/feedback` | ثبت بازخورد دستی |

## 🖥️ رابط کاربری وب

پنل «مربی پیش‌دستانه ۲۴/۷» در **تنظیمات → پیشرفته**:
- کلید فعال/غیرفعال، انتخاب شدت، روشن/خاموش کردن هر قابلیت،
- سقف پیام روزانه، نمایش زندهٔ آمار و ریسک لغزش.

## 🛠️ تنظیمات پیش‌فرض

```json
{
  "enabled": true,
  "intensity": "balanced",
  "maxPerDay": 3,
  "missPatrolEnabled": true,
  "timingEnabled": true,
  "relapseEnabled": true,
  "overdueEnabled": true,
  "goldenWindowAdvanceMinutes": 15,
  "quietHoursEnabled": false,
  "quietHoursStart": "23:30",
  "quietHoursEnd": "07:30"
}
```

## ✅ وضعیت تأیید

- `npx tsc --noEmit` → بدون خطا
- `npm run build` → موفق
- تست واقعی سرور: تشخیص صحیح افت‌ها، محاسبهٔ ریسک، و ارسال موفق پیام تلگرام تأیید شد.

---

> ⚠️ **توجه امنیتی:** فایل‌های `data/telegram_config.json` و `data/database.json` حاوی توکن ربات هستند و در `.gitignore` قرار گرفتند. هرگز آن‌ها را کامیت نکنید.
